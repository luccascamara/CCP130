// Funcao que triplica os valores apontados pelos ponteiros
void triplo(int *a, int *b, int *c) {
    *a *= 3;  // Multiplica o valor apontado por 'a' por 3
    *b *= 3;  // Multiplica o valor apontado por 'b' por 3
    *c *= 3;  // Multiplica o valor apontado por 'c' por 3
}

